use proconio::input;

fn main() {
    input! {
        m: usize,
        w: usize,
        h: usize,
        b: usize,
        d: usize,
        cgs: [(usize, usize, usize, usize, String, String); m]
    }
}
